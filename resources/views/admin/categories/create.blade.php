<x-admin-layout> <x-slot name="header"> <h2 class="page-title">Create Category</h2> </x-slot> <div class="card"> <form method="POST" action="{{ route('admin.categories.store') }}" enctype="multipart/form-data" class="p-6 space-y-4"> @csrf <div> <label class="block small fw-medium text-body">Title</label> <input name="name" value="{{ old('name') }}" class="form-control w-full mt-1" /> @error('name')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> <div> <label class="block small fw-medium text-body">Slug</label> <input name="slug" value="{{ old('slug') }}" class="form-control w-full mt-1" /> <div class="mt-1 small text-secondary">Leave blank to auto-generate from title.</div> @error('slug')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> <div> <div class="small fw-medium text-body">Icon</div> <input type="file" name="icon" class="form-control w-full mt-2" /> @error('icon')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror <div class="mt-1 small text-secondary">Square image recommended (PNG/WebP) up to 2MB.</div> </div> <label class="flex items-center gap-2 small text-body"> <input type="checkbox" name="is_active" value="1" class="form-check-input" {{ old('is_active') ? 'checked' : '' }} /> Active </label> <div class="flex items-center justify-end gap-3"> <a href="{{ route('admin.categories.index') }}" class="btn btn-outline-secondary">Cancel</a> <button class="btn btn-primary">Create</button> </div> </form> </div> </x-admin-layout> 
