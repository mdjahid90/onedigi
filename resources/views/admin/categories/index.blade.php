<x-admin-layout> <x-slot name="header"> <div class="flex items-center justify-between"> <div> <h2 class="h3 fw-semibold text-dark">Categories</h2> <div class="mt-1 small text-secondary">Organize your products by category.</div> </div> <a href="{{ route('admin.categories.create') }}" class="btn btn-primary">Create</a> </div> </x-slot> <div class="card"> <div class="table-responsive"> <table class="table table-vcenter card-table"> <thead class="bg-gray-50/70 text-secondary"> <tr> <th class="px-5 py-3.5 text-left fw-semibold">Icon</th> <th class="px-5 py-3.5 text-left fw-semibold">Title</th> <th class="px-5 py-3.5 text-left fw-semibold">Slug</th> <th class="px-5 py-3.5 text-left fw-semibold">Status</th> <th class="px-5 py-3.5 text-right fw-semibold">Actions</th> </tr> </thead> <tbody> @forelse($categories as $category) <tr> <td class="px-5 py-4"> @if(!empty($category->icon)) <img src="{{ Storage::url($category->icon) }}" alt="{{ $category->name }}" class="h-10 w-10 rounded-md object-cover border border-gray-100" /> @else <div class="h-10 w-10 rounded-md bg-gray-100 border border-gray-100"></div> @endif </td> <td class="px-5 py-4 fw-semibold text-dark">{{ $category->name }}</td> <td class="px-5 py-4 text-body">{{ $category->slug }}</td> <td class="px-5 py-4"> <span class="badge px-2.5 py-1 small fw-semibold {{ $category->is_active ? 'bg-success-lt' : 'bg-secondary-lt' }}"> {{ $category->is_active ? 'Active' : 'Inactive' }} </span> </td> <td class="px-5 py-4 text-right"> <a href="{{ route('admin.categories.edit', $category) }}" class="btn btn-primary btn-sm">Edit</a> </td> </tr> @empty <tr> <td class="px-5 py-12 text-secondary" colspan="5">No categories found.</td> </tr> @endforelse </tbody> </table> </div> </div> <div class="mt-4"> {{ $categories->links() }} </div> </x-admin-layout> 
