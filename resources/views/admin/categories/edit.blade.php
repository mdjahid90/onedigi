<x-admin-layout> <x-slot name="header"> <div class="flex items-center justify-between"> <h2 class="page-title">Edit Category</h2> <a href="{{ route('admin.categories.index') }}" class="btn btn-outline-secondary btn-sm">Back</a> </div> </x-slot> <div class="card"> <form method="POST" action="{{ route('admin.categories.update', $category) }}" enctype="multipart/form-data" class="p-6 space-y-4"> @csrf @method('PUT') <div> <label class="block small fw-medium text-body">Title</label> <input name="name" value="{{ old('name', $category->name) }}" class="form-control w-full mt-1" /> @error('name')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> <div> <label class="block small fw-medium text-body">Slug</label> <input name="slug" value="{{ old('slug', $category->slug) }}" class="form-control w-full mt-1" /> @error('slug')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> <div> <div class="small fw-medium text-body">Icon</div> <input type="file" name="icon" class="form-control w-full mt-2" /> @error('icon')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror @if(!empty($category->icon)) <div class="mt-3"> <img src="{{ Storage::url($category->icon) }}" alt="{{ $category->name }}" class="h-12 w-12 rounded-md object-cover border border-gray-100" /> </div> @endif </div> <label class="flex items-center gap-2 small text-body"> <input type="checkbox" name="is_active" value="1" class="form-check-input" {{ old('is_active', $category->is_active) ? 'checked' : '' }} /> Active </label> <div class="flex items-center justify-end gap-3"> <button class="btn btn-primary">Save</button> </div> </form> <div class="border-t border-gray-100 p-6"> <form method="POST" action="{{ route('admin.categories.destroy', $category) }}" onsubmit="return confirm('Delete this category?')"> @csrf @method('DELETE') <button class="btn btn-outline-danger">Delete</button> </form> </div> </div> </x-admin-layout> 
