<x-admin-layout> <x-slot name="header"> <div class="flex items-center justify-between gap-4"> <div class="min-w-0"> <div class="page-title text-truncate">Message: {{ $message->subject }}</div> <div class="mt-1 small text-secondary">From {{ $message->name }} ({{ $message->email }})</div> </div> <div class="shrink-0 flex items-center gap-2"> @if($message->read_at) <form method="POST" action="{{ route('admin.messages.unread', $message) }}"> @csrf <button class="btn btn-outline-secondary btn-sm">Mark unread</button> </form> @else <form method="POST" action="{{ route('admin.messages.read', $message) }}"> @csrf <button class="btn btn-primary btn-sm">Mark read</button> </form> @endif <form method="POST" action="{{ route('admin.messages.destroy', $message) }}"> @csrf @method('DELETE') <button class="btn btn-outline-secondary btn-sm" onclick="return confirm('Delete this message?')">Delete</button> </form> </div> </div> </x-slot> <div class="grid grid-cols-1 lg:grid-cols-3 gap-6"> <div class="lg:col-span-2 space-y-6"> <div class="card card-body"> <div class="small fw-semibold text-dark">Message</div> <div class="mt-4 small text-dark whitespace-pre-wrap">{{ $message->message }}</div> </div> <div class="card card-body"> <div class="small fw-semibold text-dark">Meta</div> <dl class="mt-4 grid grid-cols-1 sm:grid-cols-2 gap-4 small"> <div> <dt class="small fw-semibold text-secondary">Received</dt> <dd class="mt-1 fw-medium text-dark">{{ $message->created_at }}</dd> </div> <div> <dt class="small fw-semibold text-secondary">Read at</dt> <dd class="mt-1 fw-medium text-dark">{{ $message->read_at ?? 'Unread' }}</dd> </div> <div> <dt class="small fw-semibold text-secondary">IP</dt> <dd class="mt-1 fw-medium text-dark">{{ $message->ip_address ?? '-' }}</dd> </div> <div> <dt class="small fw-semibold text-secondary">URL</dt> <dd class="mt-1 fw-medium text-dark break-all">{{ $message->url ?? '-' }}</dd> </div> <div class="sm:col-span-2"> <dt class="small fw-semibold text-secondary">Referer</dt> <dd class="mt-1 fw-medium text-dark break-all">{{ $message->referer ?? '-' }}</dd> </div> <div class="sm:col-span-2"> <dt class="small fw-semibold text-secondary">User Agent</dt> <dd class="mt-1 fw-medium text-dark break-all">{{ $message->user_agent ?? '-' }}</dd> </div> </dl> </div> </div> <div class="space-y-6"> <div class="card card-body"> <a href="{{ route('admin.messages.index') }}" class="btn btn-outline-secondary w-full">Back to Messages</a> </div> </div> </div> </x-admin-layout> 
