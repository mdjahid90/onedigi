<x-admin-layout> <x-slot name="header"> <div class="flex items-center justify-between"> <div> <div class="page-title">Messages</div> <div class="mt-1 small text-secondary">Contact form messages from your website.</div> </div> @if($unreadCount > 0) <div class="small fw-semibold text-primary">{{ $unreadCount }} unread</div> @endif </div> </x-slot> <div class="card"> <div class="table-responsive"> <table class="table table-vcenter card-table"> <thead class="bg-gray-50/70 text-secondary"> <tr> <th class="px-5 py-3.5 text-left fw-semibold">From</th> <th class="px-5 py-3.5 text-left fw-semibold">Subject</th> <th class="px-5 py-3.5 text-left fw-semibold">Status</th> <th class="px-5 py-3.5 text-left fw-semibold">Received</th> <th class="px-5 py-3.5 text-right fw-semibold">Actions</th> </tr> </thead> <tbody> @forelse($messages as $message) @php($isUnread = $message->read_at === null) <tr class="{{ $isUnread ? 'table-active' : '' }}"> <td class="px-5 py-4"> <div class="fw-semibold text-dark">{{ $message->name }}</div> <div class="small text-secondary">{{ $message->email }}</div> </td> <td class="px-5 py-4"> <div class="fw-semibold text-dark">{{ $message->subject }}</div> <div class="mt-1 small text-secondary">{{ \Illuminate\Support\Str::limit($message->message, 120) }}</div> </td> <td class="px-5 py-4"> <span class="badge px-2.5 py-1 small fw-semibold {{ $isUnread ? 'bg-primary-lt' : 'bg-secondary-lt' }}"> {{ $isUnread ? 'Unread' : 'Read' }} </span> </td> <td class="px-5 py-4 text-body"> {{ $message->created_at?->diffForHumans() }} </td> <td class="px-5 py-4 text-right "> <a href="{{ route('admin.messages.show', $message) }}" class="btn btn-primary btn-sm">Open</a> <form method="POST" action="{{ route('admin.messages.destroy', $message) }}" class="inline"> @csrf @method('DELETE') <button type="submit" class="btn btn-outline-secondary btn-sm" onclick="return confirm('Delete this message?')">Delete</button> </form> </td> </tr> @empty <tr> <td class="px-5 py-12 text-secondary" colspan="5">No messages yet.</td> </tr> @endforelse </tbody> </table> </div> </div> <div class="mt-4"> {{ $messages->links() }} </div> </x-admin-layout> 
