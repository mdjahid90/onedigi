<x-admin-layout> <x-slot name="header"> <div class="flex items-center justify-between"> <h2 class="page-title">Banner Management</h2> <a href="{{ route('admin.marketing.banners.create') }}" class="btn btn-primary">Create</a> </div> </x-slot> <div class="card"> <div class="table-responsive"> <table class="table table-vcenter card-table"> <thead class="bg-gray-50 text-secondary"> <tr> <th class="px-4 py-3 text-left fw-medium">Image</th> <th class="px-4 py-3 text-left fw-medium">Title</th> <th class="px-4 py-3 text-left fw-medium">Link</th> <th class="px-4 py-3 text-left fw-medium">Status</th> <th class="px-4 py-3 text-right fw-medium">Actions</th> </tr> </thead> <tbody> @forelse($banners as $banner) <tr> <td class="px-4 py-3"> @if(!empty($banner->image)) <img src="{{ Storage::url($banner->image) }}" alt="{{ $banner->title }}" class="h-10 w-16 rounded-md object-cover border border-gray-100" /> @else <div class="h-10 w-16 rounded-md bg-gray-100 border border-gray-100"></div> @endif </td> <td class="px-4 py-3 fw-semibold text-dark">{{ $banner->title }}</td> <td class="px-4 py-3 text-body"> @if(!empty($banner->link)) <a class="text-primary hover:text-primary" href="{{ $banner->link }}" target="_blank" rel="noreferrer">{{ $banner->link }}</a> @else <span class="text-secondary">&mdash;</span> @endif </td> <td class="px-4 py-3"> <span class="badge {{ $banner->is_active ? 'bg-success-lt' : 'bg-secondary-lt' }}">{{ $banner->is_active ? 'Active' : 'Inactive' }}</span> </td> <td class="px-4 py-3 text-right"> <a href="{{ route('admin.marketing.banners.edit', $banner) }}" class="btn btn-primary btn-xs">Edit</a> </td> </tr> @empty <tr> <td class="px-4 py-10 text-secondary" colspan="5">No banners found.</td> </tr> @endforelse </tbody> </table> </div> </div> <div class="mt-4"> {{ $banners->links() }} </div> </x-admin-layout> 
