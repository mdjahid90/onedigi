<x-admin-layout> <x-slot name="header"> <div class="flex items-center justify-between"> <h2 class="page-title">Edit Banner</h2> <a href="{{ route('admin.marketing.banners.index') }}" class="btn btn-outline-secondary btn-sm">Back</a> </div> </x-slot> <div class="card"> <form method="POST" action="{{ route('admin.marketing.banners.update', $banner) }}" enctype="multipart/form-data" class="p-6 space-y-4"> @csrf @method('PUT') <div> <label class="block small fw-medium text-body">Title</label> <input name="title" value="{{ old('title', $banner->title) }}" class="form-control w-full mt-1" /> @error('title')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> <div> <label class="block small fw-medium text-body">Link (optional)</label> <input name="link" value="{{ old('link', $banner->link) }}" class="form-control w-full mt-1" /> @error('link')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> <div> <div class="small fw-medium text-body">Image</div> <input type="file" name="image" class="form-control w-full mt-2" /> @error('image')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror @if(!empty($banner->image)) <div class="mt-3"> <img src="{{ Storage::url($banner->image) }}" alt="{{ $banner->title }}" class="h-16 rounded-md object-cover border border-gray-100" /> </div> @endif </div> <label class="flex items-center gap-2 small text-body"> <input type="checkbox" name="is_active" value="1" class="form-check-input" {{ old('is_active', $banner->is_active) ? 'checked' : '' }} /> Active </label> <div class="flex items-center justify-end gap-3"> <button class="btn btn-primary">Save</button> </div> </form> <div class="border-t border-gray-100 p-6"> <form method="POST" action="{{ route('admin.marketing.banners.destroy', $banner) }}" onsubmit="return confirm('Delete this banner?')"> @csrf @method('DELETE') <button class="btn btn-outline-danger">Delete</button> </form> </div> </div> </x-admin-layout> 
