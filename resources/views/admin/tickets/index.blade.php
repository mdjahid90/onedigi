<x-admin-layout> <x-slot name="header"> <div> <h2 class="h3 fw-semibold text-dark">Support Tickets</h2> <div class="mt-1 small text-secondary">Manage customer support chats.</div> </div> </x-slot> <div class="card"> <div class="p-4 border-b border-gray-100"> <form method="GET" class="flex flex-wrap gap-2"> <input type="text" name="q" value="{{ $q }}" class="form-control form-control-sm w-64" placeholder="Search subject, user, email"> <select name="status" class="form-select form-select-sm"> <option value="">All status</option> <option value="open" {{ $activeStatus === 'open' ? 'selected' : '' }}>Open</option> <option value="closed" {{ $activeStatus === 'closed' ? 'selected' : '' }}>Closed</option> </select> <button class="btn btn-sm">Filter</button> </form> </div> <div class="table-responsive"> <table class="table table-vcenter card-table"> <thead class="bg-gray-50 text-secondary"> <tr> <th class="px-4 py-3 text-left fw-medium">Ticket</th> <th class="px-4 py-3 text-left fw-medium">User</th> <th class="px-4 py-3 text-left fw-medium">Priority</th> <th class="px-4 py-3 text-left fw-medium">Status</th> <th class="px-4 py-3 text-left fw-medium">Replies</th> <th class="px-4 py-3 text-left fw-medium">Last Reply</th> <th class="px-4 py-3 text-right fw-medium">Action</th> </tr> </thead> <tbody> @forelse($tickets as $ticket) <tr> <td class="px-4 py-3 fw-semibold text-dark">#{{ $ticket->id }} &middot; {{ $ticket->subject }}</td> <td class="px-4 py-3 text-body"> <div>{{ $ticket->user?->name }}</div> <div class="small text-secondary">{{ $ticket->user?->email }}</div> </td> <td class="px-4 py-3 capitalize">{{ $ticket->priority }}</td> <td class="px-4 py-3"> <span class="inline-flex rounded-full px-2 py-1 small fw-semibold {{ $ticket->status === 'open' ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-700' }}"> {{ strtoupper($ticket->status) }} </span> </td> <td class="px-4 py-3 text-body">{{ $ticket->messages_count }}</td> <td class="px-4 py-3 text-secondary">{{ $ticket->last_reply_at?->format('Y-m-d H:i') ?? '-' }}</td> <td class="px-4 py-3 text-right"> <a href="{{ route('admin.tickets.show', $ticket) }}" class="small fw-semibold text-primary hover:text-primary">Open</a> </td> </tr> @empty <tr> <td class="px-4 py-8 text-center text-secondary" colspan="7">No tickets found.</td> </tr> @endforelse </tbody> </table> </div> <div class="p-4"> {{ $tickets->links() }} </div> </div>
</x-admin-layout>

