<x-admin-layout> <x-slot name="header"> <div class="flex items-center justify-between"> <div> <h2 class="h3 fw-semibold text-dark">Products</h2> <div class="mt-1 small text-secondary">Manage your catalog items.</div> </div> <a href="{{ route('admin.products.create') }}" class="btn btn-primary">Create</a> </div> </x-slot> <div class="card"> <div class="table-responsive"> <table class="table table-vcenter card-table"> <thead class="bg-gray-50/70 text-secondary"> <tr> <th class="px-5 py-3.5 text-left fw-semibold">Image</th> <th class="px-5 py-3.5 text-left fw-semibold">Title</th> <th class="px-5 py-3.5 text-left fw-semibold">Price</th> <th class="px-5 py-3.5 text-left fw-semibold">Category</th> <th class="px-5 py-3.5 text-left fw-semibold">Status</th> <th class="px-5 py-3.5 text-right fw-semibold">Actions</th> </tr> </thead> <tbody> @forelse($products as $product) <tr> <td class="px-5 py-4"> @if(!empty($product->image)) <img src="{{ Storage::url($product->image) }}" alt="{{ $product->title }}" class="h-10 w-10 rounded-md object-cover border border-gray-100" /> @else <div class="h-10 w-10 rounded-md bg-gray-100 border border-gray-100"></div> @endif </td> <td class="px-5 py-4 fw-semibold text-dark">{{ $product->title }}</td> <td class="px-5 py-4 text-body">&#2547;{{ number_format((float) $product->price, 0) }}</td> <td class="px-5 py-4 text-body">{{ $product->category?->name ?? 'Uncategorized' }}</td> <td class="px-5 py-4"> <span class="badge px-2.5 py-1 small fw-semibold {{ $product->is_active ? 'bg-success-lt' : 'bg-secondary-lt' }}"> {{ $product->is_active ? 'Active' : 'Inactive' }} </span> </td> <td class="px-5 py-4 text-right"> <a href="{{ route('admin.products.edit', $product) }}" class="btn btn-primary btn-sm">Edit</a> </td> </tr> @empty <tr> <td class="px-5 py-12 text-secondary" colspan="6">No products found.</td> </tr> @endforelse </tbody> </table> </div> </div> <div class="mt-4"> {{ $products->links() }} </div> </x-admin-layout> 
