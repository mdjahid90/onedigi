<x-admin-layout> <x-slot name="header"> <div class="flex items-center justify-between gap-4"> <div> <h2 class="h3 fw-semibold text-dark">Product Reviews</h2> <div class="mt-1 small text-secondary">Approve or reject customer reviews before publishing.</div> </div> </div> </x-slot> <div class="space-y-4"> <div class="card p-4"> <div class="flex flex-wrap gap-2"> @php $statuses = [ 'all' => 'All', 'pending' => 'Pending', 'approved' => 'Approved', 'rejected' => 'Rejected', ]; @endphp @foreach($statuses as $value => $label) <a href="{{ route('admin.reviews.index', ['status' => $value]) }}" class="badge px-3 py-1.5 small fw-semibold {{ $activeStatus === $value ? 'bg-primary-lt' : 'bg-secondary-lt hover:bg-gray-100' }}" > {{ $label }} ({{ $counts[$value] ?? 0 }}) </a> @endforeach </div> </div> <div class="card"> <div class="table-responsive"> <table class="table table-vcenter card-table"> <thead class="bg-gray-50/70 text-secondary"> <tr> <th class="px-5 py-3.5 text-left fw-semibold">Product</th> <th class="px-5 py-3.5 text-left fw-semibold">Reviewer</th> <th class="px-5 py-3.5 text-left fw-semibold">Rating</th> <th class="px-5 py-3.5 text-left fw-semibold">Review</th> <th class="px-5 py-3.5 text-left fw-semibold">Status</th> <th class="px-5 py-3.5 text-right fw-semibold">Actions</th> </tr> </thead> <tbody> @forelse($reviews as $review) <tr> <td class="px-5 py-4"> <div class="fw-semibold text-dark">{{ $review->product?->title ?? 'Deleted Product' }}</div> @if($review->product) <a href="{{ route('products.show', $review->product) }}" target="_blank" class="small text-primary hover:text-primary">Open product</a> @endif </td> <td class="px-5 py-4"> <div class="fw-semibold text-dark">{{ $review->name }}</div> <div class="small text-secondary">{{ $review->email ?: 'No email' }}</div> <div class="mt-1 small text-secondary">{{ $review->created_at?->diffForHumans() }}</div> </td> <td class="px-5 py-4"> <span class="badge bg-warning-lt px-2 py-0.5 small fw-semibold text-warning "> {!! str_repeat('&#9733;', (int) $review->rating) !!}{!! str_repeat('&#9734;', 5 - (int) $review->rating) !!} </span> </td> <td class="px-5 py-4 text-body max-w-md"> {{ \Illuminate\Support\Str::limit($review->review, 180) }} </td> <td class="px-5 py-4"> @php $statusClass = match ($review->status) { 'approved' => 'bg-success-lt', 'rejected' => 'bg-danger-lt ', default => 'bg-warning-lt ', }; @endphp <span class="badge px-2.5 py-1 small fw-semibold {{ $statusClass }}"> {{ ucfirst($review->status) }} </span> </td> <td class="px-5 py-4 text-right "> <form method="POST" action="{{ route('admin.reviews.approve', $review) }}" class="inline"> @csrf <button type="submit" class="btn btn-primary btn-sm" {{ $review->status === 'approved' ? 'disabled' : '' }}>Approve</button> </form> <form method="POST" action="{{ route('admin.reviews.reject', $review) }}" class="inline"> @csrf <button type="submit" class="btn btn-outline-secondary btn-sm" {{ $review->status === 'rejected' ? 'disabled' : '' }}>Reject</button> </form> </td> </tr> @empty <tr> <td class="px-5 py-12 text-secondary" colspan="6">No reviews found.</td> </tr> @endforelse </tbody> </table> </div> </div> <div> {{ $reviews->links() }} </div> </div> </x-admin-layout> 
