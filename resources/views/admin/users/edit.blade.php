<x-admin-layout> <x-slot name="header"> <div class="flex items-center justify-between"> <h2 class="page-title">Edit User</h2> <a href="{{ route('admin.users.index') }}" class="btn btn-outline-secondary btn-sm">Back</a> </div> </x-slot> <div class="card"> <form method="POST" action="{{ route('admin.users.update', $user) }}" class="p-6 space-y-4"> @csrf @method('PUT') <div> <label class="block small fw-medium text-body">Name</label> <input name="name" value="{{ old('name', $user->name) }}" class="form-control w-full mt-1" /> @error('name')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> <div> <label class="block small fw-medium text-body">Email</label> <input name="email" type="email" value="{{ old('email', $user->email) }}" class="form-control w-full mt-1" /> @error('email')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> <div> <label class="block small fw-medium text-body">New Password (optional)</label> <input name="password" type="password" class="form-control w-full mt-1" /> @error('password')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> <label class="flex items-center gap-2 small text-body"> <input type="checkbox" name="is_admin" value="1" class="form-check-input" {{ old('is_admin', $user->is_admin) ? 'checked' : '' }} /> Admin </label> <div class="flex items-center justify-end gap-3"> <button class="btn btn-primary">Save</button> </div> </form> <div class="border-t border-gray-100 p-6"> <form method="POST" action="{{ route('admin.users.destroy', $user) }}" onsubmit="return confirm('Delete this user?')"> @csrf @method('DELETE') <button class="btn btn-outline-danger">Delete</button> </form> <div class="mt-2 small text-secondary">You cannot delete your own account (protected in controller).</div> </div> </div> </x-admin-layout> 
