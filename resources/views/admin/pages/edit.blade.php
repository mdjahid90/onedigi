<x-admin-layout> <x-slot name="header"> <div class="flex items-center justify-between"> <div> <h2 class="h3 fw-semibold text-dark">Edit Page</h2> <div class="mt-1 small text-secondary">Update page content and visibility.</div> </div> <a href="{{ route('page.show', $page) }}" target="_blank" class="btn btn-outline-secondary">View</a> </div> </x-slot> @push('styles') <link rel="stylesheet" href="https://unpkg.com/trix@2.1.5/dist/trix.css"> @endpush @push('scripts') <script src="https://unpkg.com/trix@2.1.5/dist/trix.umd.min.js" defer></script> @endpush <form method="POST" action="{{ route('admin.pages.update', $page) }}" class="space-y-6"> @csrf @method('PUT') <div class="card card-body space-y-5"> <div> <label class="block small fw-medium text-body">Title</label> <input name="title" value="{{ old('title', $page->title) }}" class="form-control w-full mt-1" /> @error('title')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> <div> <label class="block small fw-medium text-body">Slug</label> <input name="slug" value="{{ old('slug', $page->slug) }}" class="form-control w-full mt-1" /> @error('slug')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> <div> <label class="block small fw-medium text-body mb-2">Content</label> <input id="content" type="hidden" name="content" value="{{ old('content', $page->content) }}"> <trix-editor input="content" class="trix-content"></trix-editor> @error('content')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> <div class="grid grid-cols-1 sm:grid-cols-3 gap-4"> <label class="flex items-center gap-2"> <input type="checkbox" class="form-check-input" name="is_published" value="1" {{ old('is_published', $page->is_published) ? 'checked' : '' }} /> <span class="small fw-medium text-body">Published</span> </label> <label class="flex items-center gap-2"> <input type="checkbox" class="form-check-input" name="show_in_footer" value="1" {{ old('show_in_footer', $page->show_in_footer) ? 'checked' : '' }} /> <span class="small fw-medium text-body">Show in Footer</span> </label> <div> <label class="block small fw-medium text-body">Footer Order</label> <input name="footer_order" type="number" value="{{ old('footer_order', $page->footer_order) }}" class="form-control w-full mt-1" /> @error('footer_order')<div class="mt-1 small text-danger">{{ $message }}</div>@enderror </div> </div> </div> <div class="flex items-center justify-between"> <button type="submit" form="delete-page-form" class="btn btn-outline-secondary" onclick="return confirm('Delete this page?')">Delete</button> <div class="flex items-center gap-3"> <a href="{{ route('admin.pages.index') }}" class="btn btn-outline-secondary">Back</a> <button type="submit" class="btn btn-primary">Save Changes</button> </div> </div> </form> <form id="delete-page-form" method="POST" action="{{ route('admin.pages.destroy', $page) }}" class="hidden"> @csrf @method('DELETE') </form>
</x-admin-layout>

